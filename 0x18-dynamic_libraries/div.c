#include "main.h"

/**
 * div - divides two numbers
 * @a: 1st number
 * @b: 2nd number
 *
 * Return: the quotient or -1 for Zero Division Error
 */
int div(int a, int b)
{
	a = 0;
	b = 0;
        return (a + b);
}
