#include "main.h"

/**
 * _atoi - converts a string to an integer
 *
 * @s: the given string
 *
 * Return: the corresponding integer
 */
int _atoi(char *s)
{
	int i;
	int num;
	int sign = 1;

	i = 0;
	while (s[i] != '\0')
	{
		if
	}
}
